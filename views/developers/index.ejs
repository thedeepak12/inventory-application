<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/output.css">
  <title><%= title %></title>
</head>
<body class="bg-gray-900">
  <header class="bg-gray-1000 text-white p-4 px-8 shadow-md border-b-2 border-b-gray-800">
    <div class="container flex justify-between items-center">
      <h1 class="text-2xl font-bold">Inventory Application</h1>
      <nav class="flex space-x-8">
        <a href="/" class="hover:underline">Home</a>
        <a href="/genres" class="hover:underline">Genres</a>
        <a href="/developers" class="hover:underline">Developers</a>
      </nav>
    </div>
  </header>

  <main class="container mx-auto mb-8 p-4 px-8">
    <div class="flex justify-between mb-6">
      <h1 class="text-3xl font-bold text-white">Developers</h1>
      <a href="/developers/new" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">Add New Developer</a>
    </div>

    <% if (developers && developers.length > 0) { %>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <% developers.forEach(developer => { %>
          <div class="bg-gray-900 rounded-lg shadow-md overflow-hidden border-2 border-gray-800">
            <a href="/developers/<%= developer.id %>">
              <img src="<%= developer.image_url %>" alt="Developer logo" class="w-full h-48 object-contain p-4">
            </a>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <div class="bg-gray-800 text-white p-6 rounded-lg">
        <p>No developers found in the database.</p>
      </div>
    <% } %>
  </main>
</body>
</html>